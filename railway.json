{
    "$schema": "https://railway.com/railway.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "composer install --no-dev --optimize-autoloader --no-scripts && APP_ENV=prod php bin/console cache:warmup"
    },
    "deploy": {
        "startCommand": "php -S 0.0.0.0:8000 -t public/",
        "restartPolicyType": "ON_FAILURE"
    }
}
