{
    "$schema": "https://railway.com/railway.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "COMPOSER_ALLOW_SUPERUSER=1 composer install --no-dev --optimize-autoloader && APP_ENV=prod php bin/console cache:warmup"
    },
    "deploy": {
        "startCommand": "php -S 0.0.0.0:8000 -t public/",
        "restartPolicyType": "ON_FAILURE"
    }
}
